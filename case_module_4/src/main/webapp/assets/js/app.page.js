class App {
    static DOMAIN = location.origin;

    static BASE_URL = this.DOMAIN + "/api"
    static BASE_URL_CUSTOMER = this.BASE_URL + "/customers"
    static BASE_URL_PRIVINCE = "https://vapi.vnappmob.com/api/province/"
}